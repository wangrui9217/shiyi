const getters = {
  side_menu_open: state => state.side_menu_open,
  store_tree_list: state => state.store_tree_list,
  show_personal_center: state => state.show_personal_center
  // select_day_ts: state => state.select_day_ts,
  // room_id: state => state.room_id
}
export default getters
